const urlConstants = require('../constants/urlConstant');
const responseConstants = require('../constants/responseMessage');
module.exports = {
    urlCons: urlConstants,
    msCons: responseConstants,
}